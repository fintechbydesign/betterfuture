{
  "name": "deed.it",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "echo No build necessary on server",
    "make-ssl-certs": "sudo openssl req -x509 -nodes -days 365 -newkey rsa:2048 -keyout ./.ssl/server.key -out ./.ssl/server.crt"
  },
  "dependencies": {
    "express": "^4.16.3",
    "socket.io": "^2.0.4"
  }
}
